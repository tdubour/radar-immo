{
  "name": "radar-immo-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/finance.js && node --check src/charts.js && node --check src/radar.js && node --check scripts/collect-listings.mjs",
    "collect": "node scripts/collect-listings.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
